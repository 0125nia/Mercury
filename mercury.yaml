global:
  env: debug

discovery:
  endpoints:
    - localhost:2379
  timeout: 5

ipconf:
  service_path: /ipconf

gateway:
  workpool_num: 1024
  server_port: 9001
  epoller_channel_num: 100
  epoller_num: 4
  epoller_wait_queue_size: 100
  tcp_max_num: 10000